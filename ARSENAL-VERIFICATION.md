# Arsenal v3.2.0 verification

This protected branch must not be promoted unless GitHub Actions verifies both source-overlay checksums, compiles the pinned TOSIOS source, tests authoritative weapon fire and timed reload accounting with two synchronized clients, verifies the Outbreak starting armory and private-room hiding, preserves public classic PvP, starts the exact packaged Pterodactyl runtime, and creates an integrity-checked flat ZIP.
